Contributing
============

This repository prefers small, testable changes. When updating dependencies or
changing scraper behavior, prefer running tests locally (`pytest`) and linting
with `ruff`/`black`.

Update workflow: stash local changes, pull from origin, run tests, then apply
local changes and open a PR.
